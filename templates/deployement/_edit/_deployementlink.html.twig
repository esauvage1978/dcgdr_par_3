{% import "macros/widgets.html.twig" as widgets %}
{% import "macros/link.html.twig" as link %}
<table id="file-media" class="table table-striped table-condensed " style="width:100%">
    {% for deployementLink in item.deployementLinks %}
        <tr>
            <td>
                <a type="button" class="text-p-dark" target="_blank" href="{{ deployementLink.link }}">
                    <i class="fas fa-globe"></i>
                    {{ deployementLink.title }}
                </a>
                <br/>
                <span class="text-muted">
                    {{ deployementLink.link }}
                </span>
            </td>
            <td>
                {{ deployementLink.content | raw }}
            </td>
            <td>
                <small>
                    {{ widgets.showFullDateShortTime(deployementLink.modifyAt) }}
                </small>
            </td>
            <td class="float-right" style="width: 100px">
                {{ link.delete_btn('deployementlink',deployementLink)}}
            	{{ link.edit_btn('deployementlink',deployementLink)}}
            </td>
        </tr>
    {% endfor %}
</table>