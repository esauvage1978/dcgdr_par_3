<article id="file" class="bg-white">
	<h5 class="text-p-dark2 p-1 pt-3 text-center ">
		documents joints
	</h5>
	<div class="card-body">


		<input type="hidden" id="action_filecount" value="{{ item.actionFiles | length }}"/>
		<input type="hidden" id="action_linkcount" value="{{ item.actionLinks | length }}"/>

		<table id="file-media" class="table table-striped table-condensed " style="width:100%">
			{% for actionFile in item.actionFiles %}
				<tr id="action_media-{{ actionFile.fileName }}">
					<td>
						<a type="button" class="text-p-dark" target="_blank" href="{{ absolute_url( asset(item.actionFile.href)) }}">
							{{ file.icon(item.actionFile.fileExtension) }}

							{{ item.actionFile.title }}
						</a>
					</td>
					<td>
						{{ item.actionFile.content  | raw }}
					</td>
					<small>
						{{ widgets.showLongDate(item.actionFile.modifyAt) }}
					</small>

					<td>
						<a type="button" class="text-primary" target="_blank" href="{{ absolute_url( asset(actionFile.href)) }}">
							{{ actionFile.title }}
						</a>
					</td>
					<td class="float-right" style="width: 100px">
						<button class="actionmedia-delete btn btn-danger btn-sm " data-msg="actionFile_{{ loop.index0 }}" data-media="{{ actionFile.fileName }}">
							<i class="fas fa-trash"></i>
						</button>
						<span class="media-edit btn btn-info  btn-sm" onclick="action_showOtherFile({{ loop.index0 }})">
							<i class="fas fa-edit"></i>
						</span>
					</td>
				</tr>
				<tr id="actionFile_{{ loop.index0 }}" class="d-none">
					<td colspan="3">
						{{ form_widget(form.actionFiles[loop.index0]) }}
					</td>
				</tr>
			{% endfor %}
			{% for actionLink in item.actionLinks %}
				<tr id="action_media-{{ actionLink.id }}">
					<td>
						<a type="button" class="text-p-dark" target="_blank" href="{{ item.actionLink.link }}">
							<i class="fas fa-globe"></i>
							{{ item.actionLink.title }}
						</a>
						<br/>
						<span class="text-muted">
							{{ item.actionLink.link }}
						</span>
					</td>
					<td>
						{{ item.actionLink.content | raw }}
					</td>
					<td>
						<small>
							{{ widgets.showLongDate(item.actionLink.modifyAt) }}
						</small>
					</td>

					<td class="float-right" style="width: 100px">
						<button class="media-delete btn btn-danger btn-sm " data-msg="actionLink_{{ loop.index0 }}" data-media="{{ actionLink.id }}">
							<i class="fas fa-trash"></i>
						</button>
						<span class="media-edit btn btn-info  btn-sm" onclick="action_showOtherLink({{ loop.index0 }})">
							<i class="fas fa-edit"></i>
						</span>
					</td>
				</tr>
				<tr id="actionLink_{{ loop.index0 }}" class="d-none">
					<td colspan="3">
						{{ form_widget(form.actionLinks[loop.index0]) }}
					</td>
				</tr>
			{% endfor %}
		</table>

		<div class="row justify-content-around js-createTip-wrapper">

			<a href="#file" id="add-file" class="btn btn-p-dark mt-1">
				<i class="fas fa-plus"></i>
				ajouter un fichier
			</a>
			<a href="#link" id="add-link" class="btn btn-p-dark mt-1">
				<i class="fas fa-plus"></i>
				ajouter un lien
			</a>
			<div class="createTip ">

				<div class="createTip__title">
					Ajouter un fichier
				</div>
				<p>
					Vous devez aller sélectionner le fichier à ajouter et renseigner obligatoirement le
					<strong>le titre</strong>.
																												                    Le titre doit être explicite.
				</p>
				<div class="createTip__title">
					Ajouter un lien
				</div>
				<p>
					Lors de l'ajout d'un lien, vous devez obligatoirement renseigner :
					<ul>
						<li>
							<strong>le titre</strong>
							:
																																															il doit être explicite</li>
						<li>
							<strong>l'adresse</strong>
							mettre une URL. exemple : http://google.fr</li>

					</ul>
				</p>
				<p>
					La description du lien ou du fichier est facultative mais elle peut apporter une aide à l'utilisateur
				</p>
			</div>
		</div>

		<div id="media-new">
			<div class="row">
				<div class="col-12 media-add" id="media-add-file" data-prototype="{{ form_row(form.actionFiles.vars.prototype) | e('html_attr') }}"></div>
				<div class="col-12 media-add" id="media-add-link" data-prototype="{{ form_row(form.actionLinks.vars.prototype) | e('html_attr') }}"></div>
			</div>
		</div>


	</div>
</article>

